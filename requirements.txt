# Network Scanner — Python dependencies
# Minimum Python version: 3.10
#
# The core application and most plugins use only the standard library.
# The packages below are required only by specific plugins.
#
# Install with:  pip install -r requirements.txt

# Required by plugins/snmp.py
pysnmp>=7.0
